# Dockerfile para Frontend - Recuiva
# Sistema de Active Recall con IA - Frontend y Backend

FROM nginx:alpine

# Copiar SOLO la carpeta public (que contiene app/ y assets/)
COPY public/ /usr/share/nginx/html/

# Copiar configuración personalizada de nginx
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Exponer puerto 80
EXPOSE 80

# Comando de inicio
CMD ["nginx", "-g", "daemon off;"]
